.DS_Store
/venv/
/venv.old/
/_pycache/
*.pyc*
__pycache__/
.venv/
.vscode/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.development
.env.test
.hermes-docker/
.notebooklm-home/
.notebooklm-cli-venv/
.notebooklm-playwright/
.pip-cache/
.uv-cache/
compose.hermes.local.yml
export*
__pycache__/model_tools.cpython-310.pyc
__pycache__/web_tools.cpython-310.pyc

# API key iceren scriptler ve loglar
/set_openrouter_key.py
/set_deepseek_key.py
/set_*.py

# Yedek dosyalar
*.bak
.ReYMeN/fix_yedekleri/
/_set*.py
/_key*.py
bot_*.log
*.log
logs/
data/
.pytest_cache/
test_durations.json
.pytest-cache/
tmp/
temp_vision_images/
hermes-*/*
examples/
tests/quick_test_dataset.jsonl
tests/sample_dataset.jsonl
run_datagen_kimik2-thinking.sh
run_datagen_megascience_glm4-6.sh
run_datagen_sonnet.sh
source-data/*
run_datagen_megascience_glm4-6.sh
data/*
node_modules/
browser-use/
agent-browser/
# Private keys
*.ppk
*.pem
privvy*
images/
__pycache__/
hermes_agent.egg-info/
wandb/
testlogs

# CLI config (may contain sensitive SSH paths)
cli-config.yaml

# Skills Hub state (lives in ~/.hermes/skills/.hub/ at runtime, but just in case)
skills/.hub/
ignored/
.worktrees/
environments/benchmarks/evals/

# Web UI build output
hermes_cli/web_dist/
apps/desktop/build/
apps/desktop/dist/
apps/desktop/release/
apps/desktop/*.tsbuildinfo

# Web UI assets — synced from @nous-research/ui at build time via
# `npm run sync-assets` (see web/package.json).
web/public/fonts/
web/public/ds-assets/

# Release script temp files
.release_notes.md
mini-swe-agent/

# Nix
.direnv/
.nix-stamps/
result
website/static/api/skills-index.json
# skills.json + skills-meta.json are build artifacts emitted by
# website/scripts/extract-skills.py during prebuild — keep them out of
# git for the same reason as skills-index.json (large, generated, change
# every build).
website/static/api/skills.json
website/static/api/skills-meta.json
# automation-blueprints-index.json is a build artifact emitted by
# website/scripts/extract-automation-blueprints.py during prebuild.
website/static/api/automation-blueprints-index.json
models-dev-upstream/

# Local editor / agent tooling (machine-specific; keep in global config, not the repo)
.codex/
.cursor/
.gemini/
.zed/
.mcp.json
opencode.json
config/mcporter.json

hermes_cli/tui_dist/*
hermes_cli/scripts/
docs/superpowers/*
# Working directory for the ReYMeN Agent's session state (~/.hermes/ at runtime;
# also created in-repo when an agent operates in this checkout). Plans, audit
# logs, and per-session caches are never artifacts of the codebase.
.hermes/

# Desktop/bootstrap install marker written into the managed checkout root by the
# bootstrap installer. It is Hermes-managed runtime state, never a code change —
# ignore it so `hermes update`'s `git stash push --include-untracked` does not
# treat it as a local edit and autostash it on every run (#38529).
.hermes-bootstrap-complete

# Interrupted-update breadcrumb + recovery lock written next to the shared venv
# by `hermes update` / launch-time self-heal. Runtime state, never a code change
# — ignore so `git status` stays clean and update's autostash skips them.
.update-incomplete
.update-incomplete.lock

# Tool Search live-test harness output — non-deterministic model transcripts,
# regenerated by scripts/tool_search_livetest.py. Never an artifact of the repo.
scripts/out/

# Per-release changelog drafts. These exist only transiently during a release
# cut (passed to `gh release create --notes-file`); the GitHub Release itself
# stores the published notes. They are not a build artifact and must never be
# committed to the repo root. See the hermes-release skill.
RELEASE_v*.md

# Desktop demo-run scratch output (hermes writes demo/*.txt during recorded
# walkthroughs). Throwaway artifacts, never part of the app.
apps/desktop/demo/
nul
bot_venv/
desktop/dist/
desktop/node_modules/
.ReYMeN/memories/
.ReYMeN/stats/
.ReYMeN/skills/*/
!skills/

# ReYMeN generated files (SQLite DBs, cache, locks, logs)
.ReYMeN/session.db
.ReYMeN/hafiza.db
.reymen_hafiza/
.background_checkpoints/
.alt_ajan_hafiza/
.alt_ajan_gozlem/
.bot_direkt.lock
bot_direkt.log
REYMEN_TEST_KOMUTU.txt
cua_log.txt

# API key setup scripts (gecici, syntax bozulmaya musait)
/_env_edit.py
/_fix.py
/_fix_key.py
/_key_updater.py
/_apply_key.py
ReYMeN-full-backup/
hermes-memory-backup/

# ReYMeN özel ignore
bot_venv/
.claude/
ReYMeN-full-backup/
ReYMeN_cli/
hermes-memory-backup/

# Eski yedekler ve kalintilar
.yedek/
._yedek/
_ecc_hermes_builtin_backup/
_claude_multi_output/
bot_venv/

# GitHub 100MB limitini asan dosyalar
reymen/ReYMeN_mirror/
.ReYMeN/backups/
reymen/cereyan/.ReYMeN/skills_index.db
reymen/cereyan/.ReYMeN/ogrenmeler.db
**/chroma_db/
**/*.zip
**/*.db
.ReYMeN/hata_kodlari/
*.db
.ReYMeN/hafiza.db
.ReYMeN/memory.db
hermes_legacy/
.ReYMeN/yedekler/
.ReYMeN/yedekler

# ReYMeN venv
reymen_venv/
reymen_venv.old/

# Coverage artifacts (run-time, not code)
*.py,cover
*.cover
.coverage*
.cov_*.json
coverage/

# Geçici scriptler (anahtar yazma, token test, vs.)
_*.py
_*.bat
_*.cmd

# SQLite WAL/SHM (run-time)
*.db-shm
*.db-wal

# PID files
*.pid

# Browser/MCP state (screenshots, pages)
.playwright-mcp/

# Cache files
.ReYMeN/semantic_cache/
.ReYMeN/cron/output/

# Temp files
*.hermes-tmp.*
*.tmp

# Runtime state (regenerated)
.ReYMeN/watchdog_hash.json
**/account_summary.json
.ReYMeN/write_key.py
reymen/desktop/.server_pid
vektor_hafizasi/
chroma_db/

# Build çıktıları
*.exe
*.spec
*.egg-info/
build/
dist/
*.tsbuildinfo

# Yerel test çıktıları
wifi_cihazlari_sonuc.json
gold_price.yml
bot_*.json
test_*_sonuc.json
pyi_build*.log
bridge.log

# Video cache (binary, regenerate edilebilir)
reymen/video_cache/

# Vector cache (regenerate edilebilir)
chroma_db/
vektor_hafizasi/*.bin

# Opsiyonel/eklenti dizinleri (klonlanmaz)
optional-mcps/
plugins/
ReYMeN-memory-backup/
ReYMeN_cli/
_hermes_kopyalari/
apps/

# Runtime state files
.ReYMeN/watchdog_hash.json
**/account_summary.json
site/

# ReYMeN reference test kopyalari (Hermes Agent legacy, bagimlilik cozulemez)
tests/ReYMeN_reference/
tests/hermes_legacy/

# Runtime state: bot session, cron, learning, hata kodlari
.ReYMeN/ai_bot_*.json
.ReYMeN/ogrenmeler.jsonl
.ReYMeN/hata_kodlari/
**/hata_kodlari/
**/cron/jobs.json
**/cron/skill_sync_log.md
**/cron/output/
**/self_improvement.json
**/decisions.md
**/mcp_client.json

# Cron artifacts (runtime, regenerated)
.hermes_cron/

# Coverage (runtime)
.coverage
.cov_*.json
