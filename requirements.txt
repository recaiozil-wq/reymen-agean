# ============================================================
# ReYMeN Agent — Gereksinimler
# ============================================================
# KURULUM: pip install -r requirements.txt
# ============================================================

# ── TEMEL (zorunlu) ──
requests>=2.31.0
pyyaml>=6.0
python-dotenv>=1.0.0
psutil>=5.9.0
pillow>=10.0.0
numpy>=1.24.0
httpx>=0.28.0
rich>=14.0.0
prompt_toolkit>=3.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── LLM Provider ──
openai>=1.0.0           # DeepSeek / Groq / LM Studio
anthropic>=0.30.0       # Claude API

# ── HAFIZA / RAG ──
chromadb>=1.5.0         # Vektörel bellek

# ── DOSYA ANALİZİ ──
pdfplumber>=0.9.0       # PDF
openpyxl>=3.1.0         # Excel (.xlsx)

# ── WEB KAZIMA ──
beautifulsoup4>=4.12.0
duckduckgo_search>=8.0.0
firecrawl-py>=4.0.0

# ── TARAYICI OTOMASYONU ──
selenium>=4.15.0
playwright>=1.40.0

# ── EKRAN / OCR ──
opencv-python>=4.8.0
mss>=9.0.0
pytesseract>=0.3.10

# ── SES / TTS ──
edge-tts>=6.0.0
faster-whisper>=1.0.0

# ── TELEGRAM ──
python-telegram-bot>=20.0

# ── DISCORD ──
discord.py>=2.0.0

# ── WEB UI ──
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ── SADECE WINDOWS ──
pywin32>=306 ; sys_platform == "win32"

# ── ML (opsiyonel, torch 2GB+) ──
# torch>=2.0.0
# transformers>=4.0.0
# sentence-transformers>=2.7.0
