# ReYMeN — Example Configuration
# Copy to config.yaml and customize
agent:
  api_timeout: 60
  max_turns: 90
  tool_use_enforcement: auto

display:
  language: tr
  skin: reymen

model:
  provider: deepseek
  model: deepseek-v4-flash

terminal:
  backend: local
  timeout: 180

gateway:
  strict: false
